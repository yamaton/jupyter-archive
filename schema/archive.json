{
  "jupyter.lab.setting-icon": "jupyter-archive:archive",
  "title": "Archive",
  "description": "Archive handler options.",
  "properties": {
    "format": {
      "type": ["string", "null"],
      "enum": ["qzv"],
      "title": "Archive format",
      "description": "Archive format for compressing folder; one of ['qzv']",
      "default": "zip"
    }
  },
  "additionalProperties": false,
  "type": "object"
}
