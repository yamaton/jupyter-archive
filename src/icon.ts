import { LabIcon } from '@jupyterlab/ui-components';

// icon svg import statements
import unarchiveSvg from '../style/icons/unarchive.svg';

export const unarchiveIcon = new LabIcon({
  name: 'jupyterlab-qzv:unarchive',
  svgstr: unarchiveSvg
});
